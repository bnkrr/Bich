local addon, ns = ...
local cfg = {}

-- 总开关
cfg.enable = true

-- 位置
cfg.position = {
    x = 0,
    y = 0,
}

cfg.uiParent = ""

cfg.anchorPoint = ""

-- 图标大小
cfg.size = 32


-- 显示位置
cfg.inInstance = true
cfg.outside = false

ns.cfg = cfg